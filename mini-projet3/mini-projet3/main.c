#include <stdio.h>
#include <string.h>

    char c[100];
    char phrase [100];
    puts("hello,i am feeling happy :");
    gets(c);
    while{1}{
    puts("you:");
    gets(phrse);

    if(phrase,"exit"=null)
        puts(bot:goodbye)
        break;
    }
    if(phrase,happy!=null){
        puts("bot:hi!great to hear that!");
    }



    return 0;
}
